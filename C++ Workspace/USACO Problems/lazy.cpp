#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;

int main(){

	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
    int N, K;
    cin >> N >> K;
    int mat[N][N];
    for(int i = 0; i < N;i ++){
        for(int j = 0; j < N; j++){
            cin >> mat[i][j];
        }
    }
    int rotate[1000][1000];
    for(int i = 0; i < N;i ++){
        for(int j = 0;j < N; j++){

        }
    }

}

