#include <bits/stdc++.h>
using namespace std;
#define f first
#define s second
#define pii pair<int, int>
int N;
vector<pii> pts(205);
int main(){

	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	//int N;
	cin >> N;
	for(int i = 0;i  < N; i++){
		int a, b;
		cin >> a >> b;
		pts[i] = {a,b};
	}

}
